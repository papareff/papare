# ============================================================
# 服务器依赖清单（恒源云 / AutoDL 等 GPU 云平台）
# 版本来源：本机 RTX 3050 Laptop 已验证可用的实测版本
# ============================================================
#
# ⚠️ torch 不要通过本文件安装！
#   GPU 云平台镜像已自带匹配 CUDA 驱动的 PyTorch。
#   先确认镜像版本：
#     python -c "import torch; print(torch.__version__, torch.version.cuda)"
#   要求：torch >= 2.1，CUDA >= 11.8（本机实测 2.6.0+cu118）
#   若镜像 torch 过低，请换镜像，不要用 pip 覆盖安装。
#
# ⚠️ transformers 必须为 5.x
#   本项目代码已适配 transformers 5.x 的新 API（设备映射、量化配置传参等）。
#   装 4.x 会导致模型加载失败。
# ============================================================

# ---- 核心：模型与推理 ----
transformers>=5.0,<6.0
accelerate>=1.0
datasets>=3.0
tokenizers>=0.20
safetensors>=0.4
huggingface-hub>=0.25

# ---- 数值与数据处理 ----
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0

# ---- 可视化（服务器上出分析图时需要）----
matplotlib>=3.7

# ---- 工具 ----
tqdm>=4.65
einops>=0.7
protobuf>=4.0
